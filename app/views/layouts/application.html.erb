<!DOCTYPE html>
<html>
  <head>
    <title>PassVault</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="prose mx-auto mt-28 px-5">
    <main class="prose mx-auto mt-28 px-5">
      <%= tag.p alert, class:"bg-yellow-100 text-yellow-800 px-2 rounded" %>
      <%= tag.p notice, class:"bg-blue-100 text-blue-800 px-2 rounded" %>
      <nav class="flex items-center justify-between mb-4">
        <h1 class="pl-2 m-0"><%= link_to "PassVault", root_path, class:'font-black no-underline' %></h1>
        <div class="flex items-center gap-1">
          <% if user_signed_in? %>
            <%= link_to "Profile", edit_user_registration_path, class: "bg-gray-200 hover:bg-gray-400 cursor-pointer mt-2 border rounded px-1" %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-black-1000 cursor-pointer underline" %>
          <% else %>
            <%= link_to "Login", new_user_session_path %> |
            <%= link_to "Sign Up", new_user_registration_path %>
          <% end %>
        </div>
      </nav>
      <%= yield %>
    </main>
  </body>
</html>
